# Core model loading and training
transformers>=4.40.0        # Load pretrained models, tokenizers, chat templates
peft>=0.10.0                # LoRA / QLoRA adapter support
trl>=0.13.0                 # SFTTrainer + SFTConfig: supervised fine-tuning (0.13+ required for Transformers 5.x)
bitsandbytes>=0.43.0        # 4-bit / 8-bit quantization (the Q in QLoRA)
accelerate>=0.29.0          # Device placement, multi-GPU, mixed precision

# Data
datasets>=2.18.0            # Load datasets from HuggingFace Hub
huggingface_hub>=0.22.0     # Push models/adapters to HF Hub

# Utilities
torch>=2.2.0                # PyTorch backend
sentencepiece               # Tokenizer dependency for some models
protobuf                    # Required by some tokenizers
scipy                       # Required by bitsandbytes

# Demo
gradio>=4.26.0              # Gradio UI for the demo app

# Optional: experiment tracking
# wandb                     # Weights & Biases — uncomment if you want loss curves
